package enums;

// This defines a special type called Department.
// We use enum when we have an attribute that has only certain values.
public enum Department {
    PRODUCTION, ADMINISTRATION, SALES
}
